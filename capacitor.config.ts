import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sql-lite-cordova',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
